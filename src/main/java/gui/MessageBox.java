package gui;

import javafx.scene.layout.HBox;

/**
 * Provides a message box, which is the graphical component
 * responsible to display a single message.
 **/
public abstract class MessageBox extends HBox {
    public MessageBox(String content, String sendDate) {
        /* *
         * Task:
         * Build the message box that displays the message content and send date.
         * */
    }
}
